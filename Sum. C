#include <stdio.h>
int main() 
{
    int num, sum=0;
    printf ("enter the 10 numbers\n");
    for(int i=1;i<=10;i++)
    {
        printf("number ℅d =",i);
        scanf("℅d",&num);
        if ( num<=0)
        {
            break;
        }
        sum=sum+num;
          }
          printf("sum of the number=℅d\n",sum);
          
        
    }
